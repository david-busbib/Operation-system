A multithreaded MapReduce framework in C++ with support for custom map and reduce operations.
It allows developers to implement parallelizable tasks by defining custom key-value pairs and managing job states across map, shuffle, and reduce phases.
